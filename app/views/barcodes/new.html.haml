%body
  .row
    .col-md-7
      .widget
        %header.widget-header
          %h4.widget-title.pull-left New Barcode
          %span.pull-right
        %hr.widget-separator/
        .widget-body
          = render 'form'
      / .widget
    .col-md-5
      .widget
        %header.widget-header
          %h4.widget-title.pull-left Barcode Content Quide
          %span.pull-right
        %hr.widget-separator/
        .widget-body
          %table.table.table-bordered
            %tbody
              %tr
                %th{:scope => "row"} BOOKLAND
                %td
                  Should be a valid ISBN with / without number system or check digit.
                  E.g. 978-82-92526-14-9, 82-92526-14-9,82-92526-14
              %tr
                %th{:scope => "row"} CODE 128
                %td
                  Can be any alphanumeric or numeric-only characters. Example: ABC-abc-1234
              %tr
                %th{:scope => "row"} CODE 25 IATA
                %td
                  Should consist of decimal digits 0-9 only. Example 1023456789
              %tr
                %th{:scope => "row"} CODE 25 INTERLEAVED
                %td
                  Should consist of evenly paired decimal digits only 0-9. Example: 034597 can be paired to
                  03, 45, 97
              %tr
                %th{:scope => "row"} CODE 25 INTERLEAVED
                %td
                  Should consist of decimal digits 0-9 only. Example 1387549
              %tr
                %th{:scope => "row"} CODE 39
                %td
                  Should consist of any combination of uppercase letters (A-Z), numeric digits (0-9) and any of the
                  following special characters (-, ., $, /, +, %, and space) e.g. WIKI-980
              %tr
                %th{:scope => "row"} CODE 93
                %td
                  Should consist of any combination of uppercase letters (A-Z), numeric digits (0-9) and any of the
                  following special characters (-, ., $, /, +, %, and space) e.g. WIKI-980
                  $, /, + and %:
              %tr
                %th{:scope => "row"} EAN 13
                %td
                  Should consist of exactly 12 digits. Example 589365802134
              %tr
                %th{:scope => "row"} EAN 8
                %td
                  Should consist of exactly 7 digits. Example 5893658
              %tr
                %th{:scope => "row"} QR CODE
                %td
                  Can be any alpha-numeric string, text, url etc
              %tr
                %th{:scope => "row"} UPC SUPPLEMENTAL
                %td
                  Should be valid with 2 or 5 digits. Example 45 or 67894 are valid inputs
      / .widget
